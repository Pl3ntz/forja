# Database
DATABASE_URL=postgres://cvbuilder:changeme@localhost:5433/cvbuilder

# Auth (REQUIRED - generate with: openssl rand -hex 32)
BETTER_AUTH_SECRET=your-secret-key-at-least-32-chars-long
BETTER_AUTH_URL=http://localhost:4321

# PDF generation
PDF_CONCURRENCY=2

# Admin user (created by seed script on startup)
ADMIN_EMAIL=admin@compiler.com
ADMIN_SEED_PASSWORD=change-me-on-first-deploy

# Demo user (set SEED_DEMO=true to create demo user with sample CV data)
SEED_DEMO=false
DEMO_EMAIL=demo@cvbuilder.local
DEMO_PASSWORD=demo12341234

# Groq AI (free tier - get key from https://console.groq.com)
GROQ_API_KEY=

# Google OAuth (get from https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
